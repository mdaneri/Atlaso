# VMware and Hyper-V release smoke SSH validation on CPython 3.14.
# Keep shared cryptography and CFFI versions aligned with the release-tools lock installed
# into the same Windows candidate environment.
cffi==2.1.1
cryptography==50.0.1
paramiko>=3.5.0
